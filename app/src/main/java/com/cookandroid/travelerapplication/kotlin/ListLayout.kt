package com.cookandroid.travelerapplication.kotlin

class ListLayout(val name: String,      // 장소명
                 val road: String,      // 도로명 주소
                 val address: String,   // 지번 주소
                 val x: Double,         // 경도(Longitude)
                 val y: Double,   // 지번 주소
                 val category_group_code: String,         // 경도(Longitude)
                 val category_group_name: String)         // 위도(Latitude)
